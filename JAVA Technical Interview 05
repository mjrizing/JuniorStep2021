Q. 추상 클래스와 인터페이스의 차이
추상클래스
Default 생성자를 가진다 (서브클래스가 인스턴스화될 때 호출됨)	
추상 메소드, 비 추상 메소드를 포함할 수 있다	
추상 클래스를 상속한 클래스는 꼭 모든 메소드를 구현할 필요는 없으며, 추상 메소드만 서브 클래스 안에서 구현되면 된다.
객체 생성 불가하므로 extend (상속)로 구현	

인터페이스
생성자를 가지지 않는다
추상 메소드만 선언할 수 있다
인터페이스를 구현하는 클래스는 인터페이스에서 선언된 모든 메소드를 다 구현해야 한다.
객체 생성 불가하므로 implement 로 구현


Q. 자바의 컬렉션의 종류 아는대로
List, Set, Map 등이 있습니다. 각각의 추상화된 Collection 인터페이스 아래, 특정한 기법으로 구현된 자료구조가 들어가는데
예를들어 List는 가장 많이 사용하는 자료구조로 특정 타입 값들이 순차적으로 정렬된 컬렉션입니다. 대표적인 구현 클래스로는 ArrayList, LinkedList 등이 있습니다. 
List를 사용하면 배열 기존 사이즈에 대한 한계를 보완할 수 있습니다.
SET은 List와는 다르게 객체를 중복해서 저장할 수 없습니다. 또한 저장된 객체를 인덱스로 관리하지 않기 때문에 저장 순서가 보장되지 않습니다.
맵(Map)은 key나value 형태를 가지는 자바 컬렉션입니다. 컬렉션 프레임워크에 속하지만 List, Set과 다르게 Collection 인터페이스를 구현하지 않습니다.
